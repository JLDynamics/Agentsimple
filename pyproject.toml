[project]
name = "agentsimple"
version = "0.2.0"
description = "A compact local coding agent you can point at any project folder."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=2.38.0",
    "python-dotenv>=1.2.2",
    "rich>=15.0.0",
]

[project.scripts]
simpleagent = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = [
    "main.py",
    "agent.py",
    "config.py",
    "llm.py",
    "prompt.py",
    "sessions.py",
    "ui.py",
    "tools.py",
    "safety.py",
    "tools_schema.json",
]
